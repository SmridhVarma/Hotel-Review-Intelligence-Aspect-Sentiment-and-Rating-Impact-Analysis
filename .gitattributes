# Git LFS — track ChromaDB binary blobs
# Used in the Hugging Face Spaces repo (not the main GitHub repo).
chromadb/**           filter=lfs diff=lfs merge=lfs -text
chromadb_demo/**      filter=lfs diff=lfs merge=lfs -text
